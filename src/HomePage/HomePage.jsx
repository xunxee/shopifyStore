import styled from '@emotion/styled';

const Container = styled.div({
  flex: '1',
});

export default function HomePage() {
  return (
    <Container>
      <div>HomePage</div>
    </Container>
  );
}
