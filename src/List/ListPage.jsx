import styled from '@emotion/styled';

const Container = styled.div({
  flex: '1',
});

export default function ListPage() {
  return (
    <Container>
      <div>ListPage</div>
    </Container>
  );
}
